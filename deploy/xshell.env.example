APP_DIR=/opt/housing-platform
BACKEND_PORT=8080
FRONTEND_PORT=80

# Xshell usage:
# 1. Create an SSH session to your Linux server.
# 2. Upload this project to APP_DIR.
# 3. Run:
#    cd /opt/housing-platform
#    chmod +x deploy/remote_deploy.sh
#    APP_DIR=/opt/housing-platform BACKEND_PORT=8080 bash deploy/remote_deploy.sh
